apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gateway-api-crds
  - gateway.yaml
  # Certificate moved to infrastructure-config layer (depends on cert-manager CRDs)
  - release.yaml
